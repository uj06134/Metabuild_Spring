<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.Ex02.mapper.MemberMapper">
    <resultMap id="memberResultMap" type="com.example.Ex02.dto.MemberDto">
        <result property="hobbyAsString" column="hobby"></result>
    </resultMap>
    <select id="selectAll" resultMap="memberResultMap">
        select * from members
        <where>
            <choose>
                <when test="whatColumn == 'all' and keyword != null and keyword != ''">
                    name like '%' || #{keyword} || '%' or
                    gender like '%' || #{keyword} || '%'
                </when>
                <when test="whatColumn != null and whatColumn != 'all' and keyword != null and keyword != ''">
                    ${whatColumn} like '%' || #{keyword} || '%'
                </when>
            </choose>
        </where>
        offset #{offset} rows fetch next #{limit} rows only
    </select>
    <select id="getCount" resultType="int">
        select count(*)
        from members
        <where>
            <choose>
                <when test="whatColumn == 'all' and keyword != null and keyword != ''">
                    name like '%' || #{keyword} || '%' or
                    gender like '%' || #{keyword} || '%'
                </when>
                <when test="whatColumn != null and whatColumn != 'all' and keyword != null and keyword != ''">
                    ${whatColumn} like '%' || #{keyword} || '%'
                </when>
            </choose>
        </where>
    </select>
    <insert id="insertMember">
        insert into members
        values(#{id}, #{name}, #{password}, #{gender}, #{hobbyAsString}, #{address}, #{mpoint})
    </insert>
    <select id="findById" resultMap="memberResultMap">
        select * from members
        where id = #{id}
    </select>
    <update id="updateMember">
        update members set name = #{name}, password = #{password}, gender = #{gender}, hobby =  #{hobbyAsString}, address = #{address}, mpoint = #{mpoint}
        where id = #{id}
    </update>
    <delete id="deleteMember">
        delete members
        where id = #{id}
    </delete>
    <select id="selectCountById">
        select count(*) from members
        where id = #{id}
    </select>
    <update id="updateMpoint">
        update members
        set mpoint = mpoint + #{mpoint}
        where id=#{id}
    </update>
</mapper>